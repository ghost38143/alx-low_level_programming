#include <stdio.h>
/**
 * main - writing all the single digits
 * Return: always (success)
 */
int main(void)

{

	int i;

	for (i = 0; i <= 9; i++)
	printf("%d", i); 
	printf("\n"); 

	return (0);
}
